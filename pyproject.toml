[tool.black]
line-length = 119

[tool.pytest.ini_options]
pythonpath = [
  "."
]

[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools", "wheel"]

[tool.coverage.run]
omit = [
  "tests/*"
]